# 🚀 快速入门指南

欢迎使用 Keriko 播客系统！本指南将帮助你在 10 分钟内启动播客网站。

## ⚡ 5 分钟快速启动

### 第 1 步：安装 Hugo (1 分钟)

**Windows 用户：**

```powershell
# 使用 Chocolatey（推荐）
choco install hugo-extended

# 或使用 Scoop
scoop install hugo-extended
```

**macOS 用户：**

```bash
brew install hugo
```

**Linux 用户：**

```bash
# Ubuntu/Debian
snap install hugo

# Arch Linux
yay -S hugo
```

验证安装：

```bash
hugo version
```

应该显示 `Hugo Extended >= 0.122.0`

### 第 2 步：安装项目依赖 (1 分钟)

```bash
# 进入项目目录
cd keriko

# 初始化主题模块
hugo mod get github.com/chrede88/qubt@latest
```

### 第 3 步：启动开发服务器 (30 秒)

```bash
hugo server -D
```

看到提示后，在浏览器中访问 `http://localhost:1313`

🎉 恭喜！你的播客网站已经运行起来了！

## 📝 添加你的第一个节目

### 1. 创建节目文件

```bash
hugo new podcast/episode-001.md
```

### 2. 编辑节目信息

打开 `content/podcast/episode-001.md`，编辑以下内容：

```markdown
---
title: "我的第一期节目"
date: 2024-02-24
episode: 1
subtitle: "欢迎收听我的播客"
description: "这是我的第一期节目，介绍一下我自己和这个播客的定位"
duration: "00:20:00"
audio_file: "/audio/episode-001.mp3"
audio_size: 12000000  # 文件大小（字节）
categories:
  - 开场
tags:
  - 介绍
  - 个人成长
---

## 节目内容

欢迎收听第一期节目！今天我们来聊聊...

### 节目亮点

- 亮点一
- 亮点二
- 亮点三

## 相关链接

- 链接1
- 链接2
```

### 3. 添加音频文件

将你的音频文件放在 `static/audio/` 目录下：

```bash
# 创建音频目录（如果不存在）
mkdir -p static/audio

# 复制你的音频文件
cp /path/to/your/episode-001.mp3 static/audio/
```

### 4. 预览节目

刷新浏览器，访问 `http://localhost:1313/podcast/`，你应该能看到新添加的节目了！

## 🎨 自定义你的播客

### 修改网站信息

编辑 `config/_default/params.yaml`：

```yaml
# 修改这些内容
title: 你的播客名称
description: 你的播客描述
author: 你的名字
email: your@email.com
```

### 修改网站配置

编辑 `config/_default/hugo.yaml`：

```yaml
baseURL: 'https://yourname.gitee.io/keriko'  # 修改为你的实际 URL
languageCode: 'zh-CN'  # 修改语言代码
title: '你的播客名称'  # 修改标题
```

### 添加播客封面

将封面图片（建议 1400x1400 像素）命名为 `podcast-cover.jpg`，放到 `static/images/` 目录。

## 🚀 部署到生产环境

### 方式 1：Gitee Pages（推荐国内用户）

1. **提交代码到 Gitee**

```bash
git add .
git commit -m "Initial commit"
git push origin master
```

2. **在 Gitee 上启用 Pages**

   - 进入你的 Gitee 仓库
   - 点击 "服务" → "Gitee Pages"
   - 选择 `master` 分支和 `public` 目录
   - 点击 "启动"

3. **等待部署完成**

   几分钟后，你的网站就会在 `https://你的用户名.gitee.io/keriko` 上线！

### 方式 2：GitHub Pages（推荐国际用户）

1. **提交代码到 GitHub**

```bash
git remote add github https://github.com/yourname/keriko.git
git push github master
```

2. **在 GitHub 上启用 Pages**

   - 进入仓库 Settings → Pages
   - Source 选择 "Deploy from a branch"
   - Branch 选择 "master" 和 "/public"
   - Save

## 📱 测试播客订阅

1. **在浏览器中测试 RSS**

   访问 `http://localhost:1313/podcast/index.xml`

   应该看到 XML 格式的 RSS 订阅源。

2. **使用播客客户端测试**

   - Apple Podcast（iOS）
   - Google Podcast（Android）
   - 喜马拉雅（国内）
   - 小宇宙（国内）

   添加订阅链接：`https://你的域名/podcast/index.xml`

## 🎯 下一步

### 学习更多

- 📖 阅读 [完整文档](README.md)
- 🚀 查看 [部署指南](DEPLOYMENT.md)
- 🎨 了解 [自定义配置](#自定义你的播客)

### 定期更新

创建新节目：

```bash
hugo new podcast/episode-002.md
```

添加音频、编辑内容、提交部署，就这么简单！

### 推广你的播客

- 将 RSS 提交到播客平台（Apple Podcast、Spotify 等）
- 在社交媒体分享
- 与其他播客互推
- 建立听众社群

## ❓ 常见问题

### Q: 音频文件太大怎么办？

A: 压缩音频文件：

```bash
# 使用 ffmpeg
ffmpeg -i input.wav -b:a 64k output.mp3

# 推荐设置
# 播客：64-96 kbps（单声道）
# 音乐：128-192 kbps（立体声）
```

### Q: 如何批量导入旧节目？

A: 将旧的 Markdown 文件放到 `content/podcast/` 目录，确保每个文件有正确的 front matter。

### Q: 如何自定义样式？

A: 编辑 `assets/css/custom.css` 文件，添加你自己的 CSS。

### Q: 构建失败怎么办？

A: 检查以下几点：

1. Hugo 版本是否 >= 0.122.0
2. 是否安装了 Extended 版本
3. 主题模块是否正确初始化

```bash
# 检查版本
hugo version

# 重新初始化模块
hugo mod get github.com/chrede88/qubt@latest
hugo mod tidy
```

## 📞 需要帮助？

- 📧 邮件：podcast@keriko.com
- 🐛 问题反馈：[Gitee Issues](https://gitee.com/keriko/keriko/issues)
- 📖 文档：[完整 README](README.md)

## 🎉 开始创作吧！

现在你已经掌握了基础知识，是时候开始创作你的播客内容了！

祝你的播客之旅顺利！🎙️✨
