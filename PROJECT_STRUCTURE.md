# Keriko 播客系统 - 项目结构说明

## 📁 完整项目结构

```
keriko/
│
├── assets/                          # 资源文件目录
│   └── css/
│       └── custom.css              # 自定义 CSS 样式
│
├── config/                          # 配置文件目录
│   └── _default/                   # 默认配置
│       ├── hugo.yaml               # Hugo 主配置
│       ├── params.yaml             # 站点参数配置
│       ├── menus.yaml              # 导航菜单配置
│       └── _module.yaml            # Hugo 模块配置
│
├── content/                         # 内容目录（Markdown 文件）
│   ├── about.md                    # 关于页面
│   └── podcast/                    # 播客节目目录
│       ├── episode-001.md          # 第一期节目
│       └── episode-002.md          # 第二期节目
│
├── layouts/                         # 自定义布局模板
│   ├── _default/
│   │   └── baseof.yaml            # RSS 模板
│   └── podcast/                    # 播客专用布局
│       ├── single.html            # 单期节目页面
│       └── list.html              # 节目列表页面
│
├── static/                          # 静态资源目录（直接复制到 public）
│   ├── audio/                      # 音频文件目录
│   ├── images/                     # 图片资源目录
│   │   └── podcast-cover.jpg      # 播客封面（占位符）
│   └── robots.txt                  # 搜索引擎爬虫配置
│
├── archetypes/                      # 内容模板（创建新内容时使用）
│   └── podcast.md                  # 播客节目模板
│
├── .gitignore                       # Git 忽略文件配置
├── go.mod                           # Go 模块配置
├── go.sum                           # Go 模块校验和
├── Makefile                         # Make 构建脚本
├── README.md                        # 项目说明文档
├── QUICKSTART.md                    # 快速入门指南
├── DEPLOYMENT.md                    # 部署指南
└── PROJECT_STRUCTURE.md             # 本文件（项目结构说明）
```

## 🔑 关键文件说明

### 配置文件

#### `config/_default/hugo.yaml`
Hugo 的主配置文件，包含：
- 站点基本信息（URL、标题、语言）
- 主题设置
- 输出格式配置
- 分类法配置
- Markdown 渲染配置

#### `config/_default/params.yaml`
站点参数配置，包含：
- 播客元数据（标题、描述、作者等）
- 主题外观设置
- 社交媒体链接
- 首页和页脚配置

#### `config/_default/menus.yaml`
导航菜单配置，定义网站的主导航结构。

### 内容文件

#### `content/podcast/*.md`
播客节目的 Markdown 文件，每个文件包含：
- Front Matter（节目元数据）
- 节目内容（Markdown 格式）

#### `content/about.md`
关于页面，介绍播客的定位、使命和联系方式。

### 模板文件

#### `layouts/podcast/single.html`
单期节目页面模板，包含：
- 音频播放器
- 节目信息展示
- 章节和嘉宾信息
- 标签和分类

#### `layouts/podcast/list.html`
节目列表页面模板，显示所有节目。

### 静态资源

#### `static/audio/`
存放播客音频文件（MP3 格式）。

#### `static/images/`
存放图片资源，包括：
- 播客封面（1400x1400 像素）
- 其他相关图片

### 样式文件

#### `assets/css/custom.css`
自定义 CSS 样式，覆盖或扩展主题默认样式。

## 📝 内容组织

### 节目元数据 (Front Matter)

每个播客节目都需要包含以下元数据：

```yaml
---
title: "节目标题"              # 必需
date: 2024-02-24              # 必需
draft: false                  # 必需（是否为草稿）
episode: 1                    # 必需（期数）
subtitle: "副标题"            # 可选
description: "节目描述"        # 必需
duration: "00:30:00"          # 必需（时长）
audio_file: "/audio/..."      # 必需（音频文件路径）
audio_size: 15000000          # 必需（文件大小，字节）
audio_type: "audio/mpeg"      # 必需（MIME 类型）
categories:                   # 可选（分类）
  - 技术
tags:                         # 可选（标签）
  - 编程
guests: []                    # 可选（嘉宾）
chapters: []                  # 可选（章节）
---
```

### 分类和标签

- **Categories（分类）**：主要内容分类，如"技术分享"、"生活感悟"
- **Tags（标签）**：更具体的标签，如"Hugo"、"播客"、"JavaScript"

## 🎨 自定义指南

### 修改主题样式

1. **小调整**：编辑 `assets/css/custom.css`
2. **大改动**：复制主题模板到 `layouts/` 目录并修改

### 添加新页面

1. 创建 Markdown 文件到 `content/` 目录
2. 在 `config/_default/menus.yaml` 中添加菜单项

### 修改 RSS 订阅源

编辑 `layouts/_default/baseof.yaml` 中的 RSS 配置。

## 🔄 工作流程

### 创建新节目

```bash
# 使用 Makefile
make new TITLE=episode-003

# 或使用 Hugo 命令
hugo new podcast/episode-003.md
```

### 本地预览

```bash
# 使用 Makefile
make serve

# 或使用 Hugo 命令
hugo server -D
```

### 构建生产版本

```bash
# 使用 Makefile
make build

# 或使用 Hugo 命令
hugo --minify
```

### 部署

```bash
# 部署到 Gitee
make deploy-gitee

# 部署到 GitHub
make deploy-github
```

## 📦 依赖管理

### Hugo 模块

项目使用 Hugo 模块管理 Qubt 主题：

- **配置文件**：`go.mod`、`go.sum`
- **模块配置**：`config/_module.yaml`
- **更新主题**：`make update-theme` 或 `hugo mod get -u github.com/chrede88/qubt`

### 主题文件

主题文件自动下载到 Hugo 的模块缓存中，不需要手动管理。

## 🔍 查找文件

### 我要修改样式...
→ 编辑 `assets/css/custom.css`

### 我要添加新节目...
→ 使用 `make new` 或 `hugo new podcast/filename.md`

### 我要修改配置...
→ 编辑 `config/_default/` 下的相应配置文件

### 我要修改页面模板...
→ 编辑 `layouts/` 下的相应模板文件

### 我要添加音频...
→ 将文件放到 `static/audio/` 目录

### 我要修改导航菜单...
→ 编辑 `config/_default/menus.yaml`

## 📚 进一步学习

- [Hugo 官方文档](https://gohugo.io/)
- [Qubt 主题 Wiki](https://github.com/chrede88/qubt/wiki)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [快速入门](QUICKSTART.md)
- [部署指南](DEPLOYMENT.md)

---

如有问题，请查阅 [README.md](README.md) 或提交 Issue。
